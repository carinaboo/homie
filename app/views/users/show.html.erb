<h2><%= @user.username %></h2>
<%= image_tag @user.photo.url(:small) %>
<h3>About You: </h3>
<h4>Your Email: <%= @user.email %></h4>
<hr />
<h3>Apartment Listings You Created</h3>

<table class="apartments">
  <thead>
    <tr>
      <th></th>
      <th>Picture</th>
      <th>Title</th>
      <th>Address</th>
      <th>Bedrooms</th>
      <th>Bathrooms</th>
      <th>Price</th>
      <th>Rating</th>
    </tr>
  </thead>
  <tbody>
    <% @apartments.each do |apt| %>
      <tr>
        <td><a href="/apartments/<%= apt.id %>">View</a></td>
        <td class="picture">
          <% if apt.photo.exists? %>
            <%= image_tag apt.photo.url(:thumb) %>
          <% end %>
        </td>
        <td class="title"><%= apt.title %></td>
        <td class="street_address"><%= apt.street_address %>, <%= apt.apartment_number %>, <%= apt.city %>, <%= apt.state %> <%= apt.zip %></td>
        <td class="bedrooms"><%= apt.bedrooms.round(0) %></td>
        <td class="bathrooms"><%= apt.bathrooms.round(0) %></td>
        <td class="price"><span class="amount"><%= number_to_currency(apt.price) %></span></td>
        <td class="rating"><%= number_with_precision(apt.average_overall_rating, precision: 2) if apt.average_overall_rating %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<hr />

<h4>Your Favorite List</h4>
<table class="apartments">
  <thead>
    <tr>
      <th></th>
      <th>Title</th>
      <th>Address</th>
      <th>Bedrooms</th>
      <th>Bathrooms</th>
      <th>Price</th>
      <th>Rating</th>
    </tr>
  </thead>
  <tbody>
    <% @list.each do |apt| %>
      <tr>
        <td><a href="/apartments/<%= apt.id %>">View</a></td>
        <td class="title"><%= apt.title %></td>
        <td class="street_address"><%= apt.street_address %>, <%= apt.apartment_number %>, <%= apt.city %>, <%= apt.state %> <%= apt.zip %></td>
        <td class="bedrooms"><%= apt.bedrooms.round(0) %></td>
        <td class="bathrooms"><%= apt.bathrooms.round(0) %></td>
        <td class="price"><span class="amount"><%= number_to_currency(apt.price) %></span></td>
        <td class="rating"><%= number_with_precision(apt.average_overall_rating, precision: 2) if apt.average_overall_rating %></td>
      </tr>
    <% end %>
  </tbody>
</table>